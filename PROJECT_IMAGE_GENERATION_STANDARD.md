# 图片提示词生成器：图片生产与裁切固定规范

本文件是本项目所有生图、批量裁切、图片替换和图片接入工作的强制前置文档。每次涉及图片资源时，必须先完整读取本文件与 `skills/photo-pltb` 规则，再开始操作。

## 1. 基本目标

- 每张展示图必须准确表现其对应参数或精选提示词的实际效果，不能只做气氛相近的占位图。
- 新项目必须使用新生成的图片，不得复用其他参数、旧项目或不相关卡片的图片。
- 图片本体不烘焙参数名称。中文名、英文名和说明由界面叠加，便于维护和切换语言。
- 禁止水印、品牌标识、无关文字、乱码和不相关主体。
- 避免直接复制受版权保护的角色和品牌视觉；使用可描述的通用视觉语言。

## 2. 固定交付规格

- 参数卡最终资源统一输出为 `960 x 540`，即 `16:9`。
- 展示框尺寸保持稳定，不能因为图片数量或原图比例压缩卡片高度。
- 横图可以按安全区域适度裁切，但不能切掉参数的关键视觉证据。
- 竖图、方图或特殊比例图片必须完整缩放显示。
- 竖图两侧或方图四周的空余区域，只能使用同一张图片的模糊放大版本填充；禁止拉伸主体，禁止使用其他图片拼接。
- 最终资源自身必须干净。不能依靠界面的 `object-fit: cover` 隐藏白边、分隔线或相邻图片残片。

## 3. PHOTO_PLTB 批量生成规则

- 同类参数优先按联系表批量生成，以减少调用消耗；视觉语言差异过大的项目应拆分批次。
- 联系表提示词必须写明：实际网格、固定阅读顺序、粗分隔、安全边距、无文字、无标签、无 Logo、无水印、内容不得跨格。
- 生成后先检查模型实际输出的行列数，不能只相信提示词要求的行列数。
- 裁切必须依据实际可见边界，并在分隔线内侧保留安全内缩。项目脚本默认内缩 `12 px`，禁止使用 `0 px`。
- 若分隔线、外边距或单元格尺寸不均匀，应手动记录真实边界；禁止用理论均分结果直接覆盖正式资源。
- 原始联系表保留在生成目录，正式项目只接入经过裁切和验收的独立资源。

## 4. 新增精选提示词固定流程

用户提到“增加精选提示词”“补充精选提示词板块”或提供一组可复制提示词时，必须按以下顺序执行：

1. 先查重，再新增。
   - 检查 `frontend/src/data/featuredPrompts.ts`、`backend/catalog_seed.json`、后端数据库已有精选提示词。
   - 同时检查 `frontend/src/data/parameters.ts` 及其导入的参数库，避免和提示词生成器已有风格、用途或技法重复。
   - 重复不仅指 ID、标题完全一致，也包括作用一致、视觉结果相近、只是换了措辞的项目。
   - 若已有重复或非常接近的内容，先告知用户“已有哪一项”和“相似原因”，并等待用户确认是否仍要新增。

2. 精选提示词内容整理。
   - 没有标题时，根据提示词目标和生成结果取一个短标题。
   - 自动归类到 `调色修图`、`图生图`、`常用图片处理提示词`、`文生图` 中最合适的分类和子分类。
   - 图生图类可按实验提示词思路组织内容，但用户复制到剪贴板的最终文本不得包含“实验提示词：”前缀；可替换变量使用中文双引号标出，例如“目标风格”“目标动作”“目标城市”“主色调”。
   - 调色修图类必须强调严格基于原图，只修改色彩、曝光、光影、肤质或局部修复目标，不改变主体身份、构图和物体位置。
   - 常用处理类不需要展示图时，只保留标题、解释和复制按钮所需提示词。

3. 是否补充提示词生成器参数。
   - 若精选提示词对应的风格、媒介、用途或版式在提示词生成器中没有，应补充一个参数卡。
   - 参数卡提示词必须由视觉结果逆向提炼，只写“风格/媒介/用途本身”的关键词，不写完整工作流、比例、技能安装、上传图片处理步骤或长负面约束。
   - 若已有相同或非常接近的参数卡，不新增，只保留已有项目。
   - 风格类归入 `画面风格`；成片目标、版式模板、用途目标归入 `图片用途`；人物修图归入 `人物角色`；配色相关归入 `配色方案`。

4. 图片生成与接入。
   - 需要展示图片的精选提示词必须生成新图，不得复用旧图。
   - `调色修图` 和 `图生图` 必须有同一来源的原图和效果图，前后主体、构图和视角要能看出关联。
   - 若使用 PHOTO_PLTB 联系表，同一格只能包含一个目标图；裁切后必须确认没有相邻格内容混入。
   - 精选提示词展示图统一保存到 `public/assets/featured/`。
   - 新增参数卡展示图保存到 `public/assets/parameters/`，文件名与参数 ID 对齐。

5. 数据同步。
   - 修改前端源数据后执行目录导出，更新 `backend/catalog_seed.json`。
   - 若线上已有数据库需要自动获得新增内置数据，应增加 Django 迁移或数据导入脚本，按 `source_id` 去重插入或更新。
   - 后端迁移必须保持幂等，不能覆盖管理员后续手动编辑的无关数据。

## 5. 串图与裁切验收

每一张正式资源都必须使用完整可见模式检查四条边：

- 顶部不能出现上一行图片、横向装饰条、货架、人物局部或分隔线。
- 底部不能出现下一行图片、文字、其他主体或分隔线。
- 左右不能出现相邻单元格颜色、人物、物体或背景。
- 图片内只能有该参数需要展示的主体和环境。
- 先生成带文件名的审计拼图，再逐张检查高风险项目。
- 在网页中使用 `contain` 模式复查；`cover` 会掩盖边缘污染，不能作为验收依据。

## 6. 不同类型图片的特殊规范

### 参数卡

- 一张图只解释一个参数。
- 构图、镜头、光线、材质、妆容、服装等参数必须让差异一眼可见。
- 卡片右侧和角落要给界面控件留安全空间，不遮挡关键特征。

### 配色方案

- 每张配色卡必须在同一张横图中同时呈现“使用该配色生成的实例图”和“准确色卡”。
- 色卡建议为五色，使用确定的色值在本地程序中合成，不依赖模型随机绘制。
- 实例图必须真实使用该色组，不能只把任意旧图与色块拼在一起。

### 调色修图与图生图精选提示词

- 原图和效果图必须来自同一个源图，主体身份、构图、物体位置和视角保持一致。
- 只改变提示词要求改变的内容，例如调色、妆容、材质、风格或背景。
- 所有可替换内容使用中文双引号标出，例如“目标风格”“目标动作”“目标城市”。
- 对比图统一使用 16:9 展示；竖图完整缩放，并使用自身模糊背景填充。

### 文生图精选提示词

- 展示图必须由对应提示词实际生成，不能复用其他提示词图片。
- 提示词与展示图的主体、风格、色彩、构图和用途必须逐项一致。

## 7. 数据与缓存规则

- 文件名必须与参数 ID 完全一致，并使用项目数据所引用的扩展名。
- 新增前检查重复 ID、重复中文名、重复英文名和语义近似项目。
- 同义、作用一致或只有轻微措辞区别的项目只保留表达更清晰的一项。
- 不同参数不得指向同一张图片；新增资源必须检查文件哈希重复。
- 覆盖已发布的同名资源后，必须更新图片 URL 的缓存版本，避免浏览器继续显示旧图。
- 所有数据引用的图片必须存在，不能以通用占位图代替缺失资源。

## 8. GitHub 同步与发版流程

本项目已经迁移到 `D:\图片提示词生成器`。C 盘旧位置只保留跳转路径或兼容入口，不应再作为真实工作副本使用。所有开发、图片生成、构建、提交和推送都应在 D 盘仓库完成。

每次同步 GitHub 和触发发版时，按以下顺序一次性完成：

1. 进入真实仓库：`D:\图片提示词生成器`。
2. 运行 `git status --short`，确认只提交正式源码、迁移、种子数据和正式资源图。
3. 不提交临时检查图、生成联系表、缓存、`.env`、数据库备份、未使用 Logo 草图或本地运行目录。
4. 若远端可能有更新，先执行 `git pull --rebase origin main`，解决冲突后继续 rebase。
5. 若 Git 报 `dubious ownership`，先执行一次 `git config --global --add safe.directory D:/图片提示词生成器`。
6. 若第一次 push 因网络、认证或远端更新失败，不要改代码；先确认是否需要 rebase，然后重试同一个 `git push origin main`。本项目近期多次出现第一次网络请求失败、重试后成功的情况。
7. 推送前固定验证：
   - 前端类型检查。
   - 前端生产构建。
   - 后端迁移检查或本地迁移验证。
   - 后端测试。
   - 涉及图片时检查正式图片路径、尺寸、哈希和裁切边缘。
8. 推送成功后记录提交 hash。Cloudflare Pages 和 Railway 会根据 `main` 分支自动部署；若线上未更新，先查看平台部署日志，不要重复提交无意义空提交。

推荐命令顺序：

```powershell
Set-Location D:\图片提示词生成器
git status --short
git pull --rebase origin main
$env:Path='C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin;'+$env:Path
.\node_modules\.bin\vue-tsc.cmd --noEmit -p frontend\tsconfig.json
.\node_modules\.bin\vite.cmd build --config frontend\vite.config.ts
Set-Location D:\图片提示词生成器\backend
$env:DJANGO_USE_SQLITE='1'
.\.venv\Scripts\python.exe manage.py test content.tests content.test_photo_purposes
Set-Location D:\图片提示词生成器
git add <正式变更文件>
git commit -m "<清晰提交说明>"
git push origin main
```

若本机 MySQL 用户没有创建测试库权限，可使用 `DJANGO_USE_SQLITE=1` 验证后端代码逻辑；正式本地数据库仍使用 MySQL，并在需要时执行 `backend\.venv\Scripts\python.exe manage.py migrate`。

## 9. 固定验证流程

1. 检查本批数据 ID、名称和分类是否重复或错分。
2. 检查预期图片数量与实际输出数量一致。
3. 检查所有文件尺寸、扩展名、路径和哈希。
4. 生成带文件名的审计拼图，以完整显示方式检查每张图四边。
5. 在真实网页卡片中检查桌面宽度和较窄窗口，确认图片完整、文字不叠、卡片高度不变。
6. 运行类型检查与生产构建。
7. 仅在以上检查全部通过后，才允许提交或发布。

## 10. 近期生成经验与问题根因记录

- 旧批次曾按照理论均分网格裁切，且安全内缩为 `0`，导致部分成品顶部包含上一行单元格残片。
- 当界面使用 `object-fit: cover` 时，这些污染边缘被视觉裁掉，问题暂时不可见。
- 后续为满足竖图完整展示，主图切换为 `object-fit: contain`，隐藏问题重新暴露。
- 永久解决方式是清理资源本身、强制安全内缩、以 `contain` 模式验收，而不是恢复 `cover` 掩盖问题。
- 生成精选提示词对比图时，原图与效果图必须来自同一场景；若模型生成两张完全不相关图片，应重新生成。
- 对于竖版编辑页、海报、速写页等特殊比例，正式 16:9 展示图应使用“完整缩放 + 自身模糊背景填充”，不能裁掉主体，也不能混入其他面板。
- 新增数据后如果前端看不到后台变化，优先检查后端 API 是否返回最新数据、浏览器缓存、`catalog_seed.json` 是否已导出、线上数据库迁移是否已执行。
- 后端已有数据库不会自动读取前端 TypeScript 文件；需要通过种子导出、管理命令或 Django 数据迁移把新增内置项目写入数据库。
